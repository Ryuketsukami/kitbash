{
  "name": "@kitbash/ship",
  "version": "0.1.0",
  "description": "Ship one React (Vite) app to iOS, Android, the Microsoft Store, and the web — scaffolding plus a build pipeline over Capacitor, Electron, and PWA.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "kitbash-ship": "./dist/cli.js"
  },
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "files": ["dist", "templates", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
